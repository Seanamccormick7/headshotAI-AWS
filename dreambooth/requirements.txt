# requirements.txt

# Force the same Torch/TorchVision that we installed manually
torch==2.0.1+cu118
torchvision==0.15.2+cu118

# Keep accelerate pinned
accelerate==0.30.1

# For stable-diffusion-3.5 support, need a newer diffusers
diffusers==0.32.2

# Transformers must be >=4.31 to have BaseImageProcessor
# Often recommended to use >=4.46.0 for SD3.5 pipeline
transformers>=4.46.0

# The rest as before
ftfy
tqdm
xformers==0.0.22.post7
fastapi>=0.95.0
uvicorn>=0.21.1
pyuploadcare>=6.0.0
python-multipart
requests
pillow>=9.5.0
safetensors>=0.4.1
opencv-python-headless
bitsandbytes>=0.41.1
scipy
huggingface-hub==0.22.2
datasets
tensorboard==2.13.0
celery>=5.2.7
redis>=4.3.4
